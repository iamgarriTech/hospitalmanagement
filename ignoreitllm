Build Phase 0, then work into Phase 1 in the order in docs/roadmap.md.

Definition of done is docs/phase-1-acceptance.md — AC-1 to AC-60, negative
tests included. Postgres 18 is already running locally; never touch docker.

Don't produce a plan, a schema document, or a TODO list first. Set up the
Django project and start building. Stop when Phase 0's gate passes: a user
logs in, is refused an action they lack permission for, and both the refusal
and the successful actions are in the audit log with actor, timestamp,
resource, and before/after values.

Show me the test output.
For every session after that:


Read the code that already exists, then continue from where Phase 1 stopped.
Next unfinished slice in docs/roadmap.md order. Tell me which acceptance
criteria now pass, with test output, and which are still open.
Two things deliberately left out. No "write a plan first" —