-r requirements.txt
pytest==8.4.2
pytest-django==4.11.1
# Only for scripts/bench_inpatient.py — the load generator talks to a running
# gunicorn over HTTP, which the test client cannot do.
requests==2.34.2
